"use client";

import Header from "@/components/Header";

export default function CartPage() {
    return (
        <main className="min-h-screen bg-[#fafafa]">
            <Header />
            {/* Cart opens in the side drawer automatically when this page loads */}
        </main>
    );
}
